A demo of rails as api

## how to use

Build and install dependency:

```
$ docker compose run --rm web bin/setup
```

Start dev server:

```
$ docker compose up
```

Stop dev server:

```
$ docker compose down
```

Open console(open another terminal):

```
$ docker compose run --rm web bash
```